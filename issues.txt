fetching embeddings could be made more efficient by having each client keep up with their embeddings for 
their topics and passing them over webrtc. The issue is that embeddings are large and would require multiple
messages to send, we dont have a system to support multi-part messages yet

Need to consolidate css into reusable classes

Verify the matchmaking system is still fully functional and reliable

There were 90+ topics stuck in vector db verify cleanup

Check dark mode and verify styles look good, there is an issue with the report button in styles

Integrate a new alternative for gifs and stickers

For better ad integration we should pre-load topics onto the page
before the page is served which would allow the ad platform to pick 
up the key words and display tailored ads

CRITICAL: Need an alternate method of identifying users for vibe checks and blocking
there is a critical issue with our current IP based system that could result in 
entire TURN servers or VPNs being blocked or vibe checked

CRITICAL: Need to fix vibe check constraints, currently if someone is vibe checked
that happens silently on the server side, the user will be matched with people
who have indicated they will show nudity or gore correctly but they will be able
to report them and they will face real consequences despite doing nothing wrong

Need to set up test scripts for critical functions